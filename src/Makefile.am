lib_LTLIBRARIES = newt.la

newt_la_SOURCES =  utils.c entry.c form.c libnewt.c newt.c

# this is required to build .so with a nonstandard name, also avoid
# the numbering
newt_la_LDFLAGS= -module -avoid-version
